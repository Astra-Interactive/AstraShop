main: '${main}'
prefix: '${prefix}'
description: '${description}'
name: '${name}'
version: '${version}'
authors: [ "${authors}" ]
author: "${author}"
website: "${url}"
api-version: 1.18
softdepend: [ ]
#libraries: [ "${libraries}" ]
depend: []
loadbefore: [ ]
commands:
  ashop:
    usage: /<command>
